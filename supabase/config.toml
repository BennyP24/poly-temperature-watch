project_id = "uqcxwepisnhicoeizqqo"

# Allow browser preflight (OPTIONS) without a user JWT — client uses anon key on GET/HEAD only.
[functions.resolution-proxy]
verify_jwt = false

# Browser calls with anon key; JWT verification can reject valid anon on GET.
[functions.polymarket-proxy]
verify_jwt = false